name: Java matrix build
on:
  workflow_dispatch:

jobs:
  build-with-java:
    runs-on: arc-runner-set

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
    - name: Run java
      run: java -version
